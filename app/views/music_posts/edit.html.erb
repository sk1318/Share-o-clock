<%= form_with(model: @music_post, local: true) do |f| %>
  <div class="field">
    <%= f.text_area :content , value: @musicpost.content %>
  </div>
  <div class="field">
    <%= f.text_area :youtube , value: @musicpost.youtube %>
  </div>
  <%= f.submit "保存",class: "btn btn-primary" %>
  <% end %>